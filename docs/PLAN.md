# 🗺️ خطة التطوير الاحترافية — منصة Disor للوكلاء

> الهدف: تحويل البوت من "بوت أوامر يدير سيرفر" إلى **وكيل حقيقي كامل** — بحواس خارج ديسكورد، ذاكرة، استقلالية زمنية، وموثوقية لا تتوقف.
>
> قاعدة العمل: **مستوى واحد في كل مرة** — تنفيذ → اختبارات → مراجعة → دفع → ثم فقط الانتقال للتالي. أي تعديل يجب ألا يكسر ما بُني قبله (اختبارات المستويات السابقة تُشغَّل دائماً).

---

## ✅ ما تم إنجازه (المستوى 0 — منصة المزودين)

- طبقة مزودين كاملة: DeepSeek (passthrough أصلي 100%) + Qwen (chat.qwen.ai حقيقي) + OpenAI-Compatible
- معالج إنشاء وكيل من 3 خطوات بنوافذ مختلفة لكل مزود + إصلاح خلل تجاهل الاختيار (f61b4a2)
- أمر /المزود + تبديل حي + اختبار اتصال حقيقي + POW مخفي عن غير DeepSeek (b661814)
- 8 اختبارات E2E لسلسلة الإنشاء والواجهة

---

## 🥇 المستوى 1 — الوكيل الحقيقي (الحواس + الذاكرة + الزمن + الموثوقية)

### 1A. 🔍 أدوات ويب حقيقية
| بند | التفاصيل |
|---|---|
| `web_search` | بحث فعلي: DuckDuckGo Lite بدون مفاتيح (افتراضي) + دعم Brave/SerpAPI بمفاتيح ENV إن وُجدت. يرجع {title, url, snippet} |
| `read_url` | جلب صفحة وتنظيف HTML → نص مقروء (عنوان + محتوى حتى 8000 حرف) مع حماية: content-type، حد حجم 2MB، timeout، منع SSRF للعناوين الداخلية |
| الصلاحية | متاحة لـ member (قراءة فقط آمنة) |
| القبول | اختبارات بمحاكاة HTTP + fixtures HTML: تحليل DDG، استخراج نص، رفض غير HTML، قطع الطول |

### 1B. 🧠 ذاكرة طويلة المدى
| بند | التفاصيل |
|---|---|
| التخزين | مجموعة `agent_memories`: {agent_id, guild_id, user_id, kind, content, tags, hits, timestamps} |
| الأدوات | `remember` (يضيف حقيقة، dedup + حد 200/مستخدم)، `recall` (بحث بالكلمات + تعزيز حداثة)، `forget` (حذف بمعرف أو استعلام) |
| الحقن التلقائي | قبل كل محادثة: أعلى 12 ذكرى للمستخدم تُحقن في السياق |
| الصلاحية | member لكنه محصور بذكرياته هو فقط |
| القبول | اختبارات: dedup، الحد الأقصى، ترتيب الصلة، النسيان، عزل المستخدمين |

### 1C. ⏰ محرك تذكيرات
| بند | التفاصيل |
|---|---|
| التخزين | مجموعة `agent_reminders`: {agent_id, guild_id, channel_id, user_id, text, due_at, recurrence} |
| الأدوات | `set_reminder` (بعد-دقائق / وقت-ISO / يومي HH:MM / أسبوعي يوم+وقت)، `list_reminders` (تذكيراته هو)، `cancel_reminder` |
| المحرك | ماسح كل 20 ثانية داخل runtime الوكيل، إرسال بقناة الطلب عبر عميل الوكيل نفسه، إعادة جدولة التكرار، التقاط المفقود عند الإقلاع |
| القبول | اختبارات: كل صيغ الوقت، دورة المحرك بتوقيت وهمي، التكرار، catch-up |

### 1D. 🔄 Fallback بين المزودين
| بند | التفاصيل |
|---|---|
| المنطق | فشل المزود الأساسي (شبكة/RateLimited/حظر) → تجربة المزود التالي في السلسلة تلقائياً لنفس الطلب، جلسة جديدة للمزود البديل، واستكمال بقية خطوات الحلقة به |
| الإعداد | لكل وكيل: `fallback_enabled` + `fallback_chain` (ترتيب) — واجهة في صفحة المزود باللوحة (إضافة/إزالة/تفعيل) والتحقق من جاهزية كل مزود |
| الشفافية | سجل في agent_logs + سطر في الكونسول؛ المستخدم يرى الرد طبيعياً |
| القبول | اختبار: مزود أساسي يفشل → البديل يجيب، ولا يتغير سلوك عدم فشل الأساسي |

### 1E. 🎨 تجديد الأوامر والتصميم
| بند | التفاصيل |
|---|---|
| نظام تصميم موحد | دالة embed مركزية: ألوان دلالية، ترويسة بأيقونة، فوتر باسم الوكيل + وقت، فواصل نصية ثابتة |
| أمر `/مساعدة` | قائمة كاملة مصنفة (محادثة/إدارة/ويب/ذاكرة/تذكير/مزود) بوصف موجز لكل أمر |
| تنظيف الأوصاف | توحيد أسلوب وصف كل أوامر السلاش عربياً، والردود المدمجة (ephemeral) حيث يلزم |
| القبول | مراجعة بصرية للـ embeds عبر اختبار بنية الحقول |

---

## 🥈 المستوى 2 — النضج ✅ (مكتمل)

> **منجز بالكامل (v7.3.0 "Sentinel")**: صفحة الإعدادات الكاملة (عرض/كشف آمن/تعديل مجزأ + تعطيل web_search)، الشخصية من ملف، قاعدة معرفة RAG بأدوات search_knowledge/list_knowledge، تتبع الاستخدام مع صفحة إحصائيات لكل وكيل، الاستباقية بتهدئة لكل قناة، تشفير AES-256-GCM للتوكنات مع ترحيل شفاف عند الإقلاع. 118 اختباراً (16 E2E + 102 وحدة).

### 2A. ⚙️ صفحة إعدادات الوكيل الكاملة — عرض + تعديل (طلب مباشر)
| بند | التفاصيل |
|---|---|
| المشكلة | لا يمكن رؤية إعدادات وكيل قائم (اسمه/توكنه/توكن نموذجه/مزوده) — الإنشاء فقط |
| الصفحة | `renderAgentSettings`: كل الحقول مع إخفاء الأسرار (`••••1234`) + زر «كشف» يرسل القيمة الحقيقية رسالة ephemeral لا يراها غير طالبها |
| التعديل | نوافذ تعديل مجزأة: الاسم + الشخصية / توكن ديسكورد / بيانات المزود (حقول المزود نفسه) — تُحدث قاعدة البيانات + الـ runtime الحي فوراً |
| تفعيل/تعطيل الأدوات | لكل وكيل: `features.web_search` (افتراضي مفعّل) — لأن كل نموذج أصبح يملك بحثاً مدمجاً، البحث الخارجي قد لا يلزم → يُعطَّل من إعدادات الوكيل |
| القبول | اختبار: الصفحة لا تعرض أي سر خام، زر الكشف يعمل، التبديل يُخزن ويُحدّث حياً، runAgent يرفض web_search عند التعطيل |

### 2B. 📎 الشخصية من ملف (طلب مباشر)
| بند | التفاصيل |
|---|---|
| التدفق | منشن الوكيل + كلمة «شخصية» + إرفاق ملف .txt/.md (≤ 1MB، ≤ 20000 حرف) → يقرأ الملف ويستبدل الشخصية في القاعدة والـ runtime الحي |
| الصلاحية | أدمن/مالك فقط — الأعضاء لا يستطيعون تغيير شخصية الوكيل |
| القبول | اختبار وحدة للتحليل والتحقق (نوع الملف، الحجم، الصلاحية) + تحديث حي |

### 2C. 📚 قاعدة معرفة RAG لكل وكيل
| بند | التفاصيل |
|---|---|
| التخزين | مجموعة `agent_knowledge` (مُعدة في config.js): {agent_id, guild_id, source, chunk_index, content, tokens, created_at} |
| الرفع | من صفحة المعرفة باللوحة: زر «إضافة ملفات» → أرسل الملفات في القناة خلال 3 دقائق → Manager يلتقطها (.txt/.md/.json وغيرها من الامتدادات النصية، ≤ 1MB) |
| التقطيع | تقطيع واعٍ بالفقرات (~900 حرف، تداخل 120) + فهرسة بالكلمات (نفس tokenizer الذاكرة) |
| الأدوات | `search_knowledge` [query, source, limit] + `list_knowledge` — للأدمن/المالك فقط (ليست في MEMBER_SAFE_TOOLS) |
| القبول | اختبارات: التقطيع والفقرات الطويلة، الترتيب بالصلة، حذف مصدر، عزل الوكلاء |

### 2D. 📊 تتبع الاستخدام + إحصائيات لكل وكيل
| بند | التفاصيل |
|---|---|
| التخزين | مجموعة `agent_usage`: وثيقة لكل {وكيل، سيرفر، يوم} مع $inc — messages / tool_calls / web_calls / provider_calls لكل مزود / fallbacks / errors |
| الربط | hooks في agentRuntime (كل رسالة مستخدم تُعالج) وrunAgent (كل استدعاء أداة، كل تبديل fallback، كل نجاح مزود) |
| الصفحة | زر «الإحصائيات» في صفحة الوكيل: اليوم/7 أيام/30 يوم — رسائل، أدوات (مفصلة top 5)، استدعاءات المزودين، الأخطاء، الإشغال اليومي (مخطط أعمدة نصي) |
| القبول | اختبارات: $inc تصاعدي، فصل الأيام، ملخص صحيح، أمان بدون DB |

### 2E. 🤖 الاستباقية — إصغاء قنوات
| بند | التفاصيل |
|---|---|
| الإعداد | لكل وكيل: `proactive_enabled` + `proactive_channels: [{channel_id, keywords[], cooldown_minutes}]` — من اللوحة |
| العمل | رسالة غير موجّهة له في قناة مُصغاة + تطابق كلمة مفتاحية + انتهاء التهدئة → يُعالجها كمحادثة كاملة (بدون منشن) |
| الأمان | تهدئة لكل قناة (1..720 دقيقة، افتراضي 10)، تجاهل البوتات، لا استباقية في الخاص |
| القبول | اختبار وحدة: مطابقة الكلمات، التهدئة، تجاهل غير المطابق |

### 2F. 🛡️ تشفير التوكنات — AES-256-GCM
| بند | التفاصيل |
|---|---|
| المفتاح | ENV `ENCRYPTION_KEY` (hex 64 أو أي عبارة → اشتقاق scrypt) — بلا مفتاح: تحذير والتخزين كما كان (قابل للتعطيل) |
| الصيغة | `enc:v1:<iv>:<tag>:<data>` — بادئة تُميّز المشفر عن القديم |
| الترحيل | عند الإقلاع: كل التوكنات النصية (discord_token, deepseek_token, qwen_token, openai_api_key) تُشفّر مرة واحدة (idempotent) |
| القراءة | فك شفاف عند كل نقطة استخدام (runtime/لوحة/اختبار اتصال) — العرض دائماً مقنّع ما عدا زر الكشف |
| القبول | اختبارات: roundtrip، عبث بالبيان يُفشل، عبارة pass-through، ترحيل لا يكرر |

### 🎨 تجديد الأوامر (مستمر)
- أوامر runtime جديدة: `/الميزات` (عرض/تبديل web_search)، `/الاحصائيات` (إحصائيات الوكيل)
- /اوامر و/مساعدة: قسم جديد يشرح الذاكرة الدائمة (كيف تعمل فعلاً) + رفع شخصية من ملف + قاعدة المعرفة + الاستباقية

## ⚡ المستوى 2.5 — منصة المزودين 2.0 (v7.4.0 "Nexus") — قيد التنفيذ

> مبنية على طلبات المستخدم المباشرة + ملف البروكسي المرفق (Universal AI Proxy v10.0) و qwen.py في المستودع.
> المبدأ الجديد: **نعتمد على قدرات النموذج الأصلية (تفكير/بحث مدمج) — لا على أدوات متصفح خارجية.**

### 2.5-A. 🗑️ حذف بحث المتصفح (web_search) نهائياً — طلب صريح
| بند | التفاصيل |
|---|---|
| الحذف | أداة `web_search` تُزال كلياً (webTools/agent loop/systemPrompt/القوائم/الأوامر) — البحث مسؤولية النموذج نفسه (بحثه المدمج) |
| `read_url` يبقى | قراءة رابط يلصقه المستخدم في ديسكورد ليست بحثاً — تبقى خلف مفتاح `features.read_url` (افتراضي مفعّل، قابل للتعطيل من الإعدادات) |
| القبول | لا أثر لـ web_search في الكود والاختبارات؛ read_url يُرفض عند تعطيله برسالة واضحة |

### 2.5-B. 🧠 قدرات النماذج الأصلية لكل وكيل: التفكير + البحث المدمج — طلب صريح
| بند | التفاصيل |
|---|---|
| الإعداد | `capabilities: { thinking, search }` لكل وكيل (DB + runtime حي + لوحة + /الميزات) |
| Qwen | `feature_config.thinking_enabled/auto_thinking` + `auto_search` (كما في qwen.py v10) |
| DeepSeek | `thinking_enabled` (موجود) + `search_enabled` جديد في payload (كما في البروكسي) |
| OpenAI-Compatible | التفكير: `reasoning_effort: medium` مع إعادة محاولة تلقائية بدونه إن رفضه المزود (400) — البحث: حسب المزود نفسه |
| الجلسات | تفكير جلسة القناة (محادثة-جديدة تفكير:on) يظل الأعلى أولوية؛ قدرة الوكيل هي الافتراضي |
| القبول | الحمولات تُبنى بالأعلام الصحيحة لكل مزود (اختبارات) + تبديل حي من اللوحة والأمر |

### 2.5-C. 🌐 مزود Gemini جديد (كوكيز وليس توكن) — من البروكسي v10 — طلب صريح
| بند | التفاصيل |
|---|---|
| المصادقة | سلسلة كوكيز كاملة من المتصفح (`gemini_cookies`) — سر مشفر — تُعالج مختلفاً عن التوكنات |
| التدفق | استخراج SNlM0e/FdrFJe من صفحة gemini.google.com → StreamGenerate → تتبع conversation/response/choice ids + at_token → تحديث الكوكيز الدوارة |
| الجلسات | ذاكرة RAM لكل وكيل+قناة (TTL 6 ساعات) بنفس عقد المزودين (sessionId/parentMessageId) |
| القبول | اختبارات بخادم وهمي: استخراج التوكنات، تفكيك الرد المتدفق التزايدي، إعادة استخدام الجلسة |

### 2.5-D. 🎨 Qwen تكامل كامل (qwen.py + البروكسي) — طلب صريح
| بند | التفاصيل |
|---|---|
| رؤية الصور | مرفق صورة في رسالة منشن الوكيل → رفع إلى Qwen OSS (getstsToken → multipart → complete) → files payload — النموذج يرى الصورة |
| توليد الصور | أداة `generate_image` (t2i) للوكيل — تجلب رابط/ملف الصورة وترسله في ديسكورد (مدعوم مع Qwen) |
| القبول | استخراج روابط الصور من الـ stream، بناء files payload، أداة generate_image في الحلقة والبرومبت |

### 2.5-E. 🗄️ قاعدة المزودين المحفوظين + أوامر إدارتهم — طلب صريح
| بند | التفاصيل |
|---|---|
| التخزين | مجموعة `ai_providers`: {name, base_url, api_key (مشفرة), models[], created_by, created_at} |
| المعالج التفاعلي | خطوات فعلية: نافذة (الاسم + base_url + مفتاح + نماذج اختياري) → اختبار اتصال حقيقي وجلب /models تلقائياً → اختيار النماذج من القائمة → حفظ |
| الأوامر | `/اضافة-مزود` (يفتح المعالج) + `/المزودون` (عرض/حذف المزودين المحفوظين) — من أوامر المدير |
| القبول | المفتاح يُخزن مشفراً؛ المعالج ينجح بلا /models؛ الحذف بتأكيد |

### 2.5-F. ➕ إنشاء وكيل بمزود محفوظ — بدل كتابة البيانات يدوياً — طلب صريح
| بند | التفاصيل |
|---|---|
| التدفق | عند اختيار OpenAI-Compatible في المعالج: إن وُجد مزودون محفوظون تظهر قائمة للاختيار من قاعدة البيانات (ثم اختيار النموذج من نماذجه) — وإلا نافذة الإدخال اليدوي كما كان |
| النسخ | base_url + api_key تُنسخان من وثيقة المزود المحفوظ (المفتاح يبقى مشفراً حتى الحفظ) |
| القبول | E2E: اختيار مزود محفوظ → وكيل يُنشأ بإعداداته بدون كتابة أي بيانات |

### 2.5-G. 📎 الشخصية من ملف — التدفق الصحيح (زر ينتظرك) — طلب صريح
| بند | التفاصيل |
|---|---|
| المشكلة | النافذة (Modal) لا تقبل مرفقات أصلاً — لا معنى لطلب «ضع ملفاً» داخل نموذج |
| التدفق | زر «شخصية من ملف» في صفحة الإعدادات → رسالة «أرسل الملف الآن» تنتظر 3 دقائق → عند إرفاق .txt/.md (≤1MB، ≤20000 حرف) يصبح محتواه شخصية الوكيل فوراً (DB + runtime حي) |
| القبول | E2E للالتقاط: ملف صحيح يُحفظ، غير نصي يُرفض، انتهاء النافذة يُتجاهل |

### 2.5-H. 🛠️ إصلاح /المزود ورسائل الأخطاء — طلب صريح
| بند | التفاصيل |
|---|---|
| السبب الجذري | أوامر الأدمن كانت تنهار عند غياب member/guild (خاص) لأن `member.permissions` يُقرأ دون حماية → «حدث خطأ أثناء معالجة الأمر» لأغلب الأوامر |
| الإصلاح | حارس موحد: أدمن السيرفر أو مالك البوت، ورسالة واضحة في الخاص؛ معالج الأخطاء يعرض السبب الحقيقي المقتطع بدل الرسالة العامة |
| القبول | /المزود يعمل في الخاص والسيرفر؛ كل الأوامر المحمية تستخدم الحارس نفسه |

---

## 🥉 المستوى 3 — الاحتراف الكامل

- 📦 **قوالب وكلاء**: تصدير/استيراد JSON كامل (شخصية/مزود/سلسلة fallback) من اللوحة
- ✍ية **رد متدفق**: onDelta في عقد المزودين + محرر رسالة ديسكورد بمعدل آمن (تحديث كل ~1.5 ث)
- 🎭 **مكتبة شخصيات**: بدائل جاهزة تُطبق على الوكيل بضغطة

---

## 📏 معايير ثابتة لكل مستوى

1. لا اختراع أصناف جديدة للمزودين — كل شيء عبر عقد providers
2. كل مجموعة Mongo جديدة تُدار من وحدة واحدة (بدون استعلامات مبعثرة)
3. `node --check` لكل الملفات + كل اختبارات المستويات السابقة تمر قبل الدفع
4. توافق قديم إلزامي: وكيل بلا إعدادات جديدة يعمل كما كان حرفياً
5. كل ميزة قابلة للتعطيل (ENV أو إعداد لكل وكيل) — لا إجبار

---

## ✅ v7.11.0 "La Yansaa" — لا يُنسى ما عاد يُنسى (طلب المالك)

### 1. 🧷 ذاكرة القنوات الدائمة — نهاية «ينسى كل شيء»
| بند | التفاصيل |
|---|---|
| المشكلة | أي فشل/تبديل توكن/تبديل مزود = محادثة جديدة = النموذج ينسى كل ما دار في القناة |
| الحل | مجموعة `channel_history` لكل (وكيل+سيرفر+قناة): كل رسالة وكل رد يُلحق بها، وعند **كل** بداية جلسة جديدة (فشل، مفتاح جديد، مزود جديد، إعادة تشغيل) يُحقن آخر 24 حدثاً في السياق تلقائياً |
| التصفير | /محادثة-جديدة و/حذف-محادثة يمسحان السجل الدائم أيضاً (بداية نظيفة حقيقية) |
| العزل | وكيل لا يرى ذاكرة قنوات وكيل آخر أبداً |
| الملفات | channelHistory.js جديد + حقن في tools/agent.js + تسجيل من agentRuntime |

### 2. 🔑 مفاتيح متعددة لكل مزود + سلّم التعافي
| بند | التفاصيل |
|---|---|
| الإدخال | نفس الحقل يقبل عدة توكنات/كوكيز مفصولة بسطر أو \| (وفاصلة للمفاتيح النصية؛ الكوكيز محمية من الفاصلة) |
| السلّم | خطأ → محادثة جديدة + إعادة الطلب (×2) → 🔑 المفتاح التالي (لكل مفتاح ×3 محاولات) → 🌐 المزود التالي في سلسلة الوكيل → وجه بشري للقناة + تقرير كامل للإشعارات |
| الذاكرة | كل قفزة في السلّم تحقن ذاكرة القناة — الحوار يكمل بدون نسيان مهما تبنت |
| الوصف | describe لكل مزود يعرض عدد المفاتيح «(N مفاتيح بديلة 🔑)» |
| التتبع | usage: retry / key_rotate / fallback / error منفصلة |

### 3. 🙈 حرية التجاهل + 😄 تفاعل الإيموجي + 🧍 بشرية الحوار
| بند | التفاصيل |
|---|---|
| التجاهل | النموذج يرد `IGNORE_MSG` وحدها → صفر رد في القناة، النظام يضع 🙈 على رسالة المستخدم (النموذج لا يُجبر على الرد غصباً) |
| التفاعل | `{"tool":"react","params":{"emoji":"🔥"}}` → تفاعل بلا نص، أو تفاعل + رد معه؛ تحقق صارم من الإيموجي |
| البشرية | برومبت «بشرية الحوار»: طول الرد بقدر الرسالة، عامية مع العامية، بلا قوائم للدردشة، بلا افتتاحيات مساعدات + إيقاع زمني طبيعي (وقت قراءة + كتابة متغير) قبل إرسال الرد |

### 4. 🛠️ إصلاحات الجودة
| المشكلة | الإصلاح |
|---|---|
| /الرصد خطأ أحمر | deferReply فوري قبل أي استعلام + تجميع إحصائيات النشاط في استعلام واحد + دفعة حسابات Qwen + ترقيم صفحات 8 سيرفرات/صفحة |
| الذاكرة «لا تُستخدم» | حفظ تلقائي حتمي: «تذكر أنني…» يُحفظ من النظام مباشرة حتى لو لم يستدعِ النموذج أداة + تعزيز البرومبتات (احفظ فوراً/استدعِ recall قبل «ماذا تعرف عني») |
| التذكيرات لا تشتغل | احتياطي DM إذا فشلت القناة + محرك تذكيرات احتياطي في بوت المدير يغطي تذكيرات الوكلاء المتوقفين/الفاشلين |

**الاختبارات**: recovery_ladder.test.js جديد (9) — كل المجموعة خضراء، exit 0.

---

## v7.12.0 — «Radar Jadid» (الجولة السادسة عشرة)

### 1. 🎭 قائمة الانتظار لكل وكيل — لا انتظار بين الوكلاء
| بند | التفاصيل |
|---|---|
| الشكوى | «النظام على الوكلاء كلها — لو كلمني وكيلاان بنفس الوقت ينتظر أحدهما الآخر» |
| الجذر | مفتاح القائمة كان guildId_channelId — والقوائم مشتركة عالمياً بين كل عمليات التشغيل (require cache) |
| الإصلاح | agentChannelKey(agentId, guildId, channelId) — البانِر الوحيد للمفاتيح؛ كل وكيل قائمته المستقلة حتى في نفس القناة |
| الضمانة | نفس الوكيل + نفس القناة = انتظار صارم بالترتيب (واحد واحد بس محفوظ) + وكيلاان مختلفان = توازٍ كامل |

### 2. 🛰️ /الرصد بإعادة تصميم كاملة — «اختر وكيلك أولاً»
| بند | التفاصيل |
|---|---|
| الشكوى | «التصميم خرا حرفياً — لا أفهم شيء» (كل السيرفرات مختلطة بكل الوكلاء في صفحة واحدة) |
| التدفق الجديد | /الرصد → قائمة اختيار الوكيل → سيرفرات هذا الوكيل فقط، كل سيرفر كتلة مستقلة |
| الفصل البصري | خط مرئي حقيقي (Separator divider من ديسكورد نفسه) بين كل سيرفر + Markdown نظيف (### عنوان + أسطر مفاتيح + اقتباس للمعرفات) |
| المعطيات لكل سيرفر | الحالة، الأعضاء، من أضافه ومتى، مالك السيرفر، محادثات هذا الوكيل تحديداً، آخر نشاط، حالة Qwen، من يتكلم الآن |
| عزل البيانات | getAgentRadar(agentId, botUserId, agentName) — سيرفرات عبر apps + نشاط الوكيل، وإحصائيات نبضاته هو فقط، والنبضة الحية لوكيل آخر لا تظهر على وكيله |
| خرائط الهوية | discord_bot_id يُحفظ على وثيقة الوكيل عند كل ready + recordGuildJoin يسجل apps فوراً (كان ينتظر إقلاعاً جديداً) |
| التنقل | تحديث/سابق/تالي/تغيير الوكيل + زر تفاصيل لكل سيرفر → صفحة السيرفر ترجعك لسيرفرات نفس الوكيل (توافق الصيغة القديمة محفوظ) |

**الاختبارات**: radar_agent.test.js جديد (6) + channel_queue 7 — كل المجموعة خضراء (22 سويت، exit 0).

---

## v7.13.0 — «Bila Admin» (الجولة السابعة عشرة)

### 🛡️ الوكيل يعمل بدون رتبة Administrator — مضمون بالتصميم
| بند | التفاصيل |
|---|---|
| الشكوى | «لو عملت وكيل بحساب وكان رتبته ليست أدمن ستريتر لا أستطيع التكلم معه — يضع إيموجي فقط ولا يرد. لا يُشترط أدمن لكي يعمل!» |
| الجذر 1 | صلاحية «إرسال الرسائل» مفقودة في القناة (بلا أدمن لا يوجد تجاوز): الرد كان يُولَّد بالذكاء الاصطناعي **أولاً** ثم message.reply() يرمي DiscordAPIError[50013] — فيرى الناس إيموجي فقط بلا رد وبلا سبب |
| الجذر 2 | مهمة عالقة (مزود لا يستجيب) تحتجز قائمة القناة للأبد → كل رسالة تالية تحصل على 👀 إلى الأبد |
| **الفحص المبكر** | قبل أي استهلاك للـ AI: checkReplyAbility تتحقق من «عرض القناة + إرسال الرسائل» — إن نقصت: 🔇 على الرسالة + تقرير فوري للمالك بالصلاحيات الناقصة بالعربية وخطوة الإصلاح — **صفر هدر ذكاء اصطناعي وصفر انسداد قائمة** |
| **قاعدة بلا أدمن** | الحد الأدنى فقط (عرض+إرسال) يكفي — قراءة السجل/التفاعلات تحذيرات لا حجب — غياب المعلومات fail-open (لا صمت خاطئ أبداً) — حسابات user لا تُفحص (تعمل بصلاحيات الحساب البشري) |
| **ساعة التوقف** | withTaskTimeout حول runAgent (180 ث): مهمة عالقة تُحرر القائمة حتماً + ⏰ + تقرير «قائمة الانتظار حُررت تلقائياً» — العمل المتأخر قد يكمل بالخلفية (فشله المتأخر مبتلع بلا unhandled) |
| **كشف 50013** | المصيد يميز Missing Permissions بأي شكل (code/اسم/نص) → 🔐 مميز بدل ❌ + تقرير source:permissions بالنص الحاسم |
| **/شرح تشخيصي** | البطاقة تعرض الآن فحصاً حياً في قناة الأمر: ✅/❌ لكل صلاحية + «سبب صمتي هنا!» + «الأدمن غير مطلوبة إطلاقاً» + خطوة الإصلاح — بلا تشخيص تبقى البطاقة الأصلية حرفياً |
| مصادر التقارير | errorReporter: source 'permissions' (لا يملك صلاحية الرد — الأدمن غير مطلوب) + 'timeout' (تجاوز الحد الزمني وحُررت القائمة) |

**الاختبارات**: permissions.test.js جديد (11): الحد الأدنى يكفي بلا أدمن، الحجب المبكر بالتسمية العربية، fail-open، كشف 50013 بأي شكل، **إعادة إنتاج انسداد القائمة وإصلاحه فعلياً** (عالق أبدياً ← الساعة تُحرر والرسالة التالية تمر)، بطاقة التشخيص بالصيغتين، التقارير للمالك، وعقد المصدر (البوابة قبل النص والقائمة). المجموعة الكاملة: 23 سويت كلها خضراء (exit 0).

## v7.14.0 — «الوكيل يلعب» 🎮 — نقل نظام Auto كاملاً (طلب المالك الثامن عشر)

**المصدر**: مستودع المالك zeus12345king/Auto — نزّلناه وقرأناه ملفاً ملفاً وأخذنا منه «كل شيء فيما يخص الألعاب وكيفية دخولها والتفاصيل» حرفياً، كما أمر: «اعتقد اننا سوف ناخذ كل شيء منه بالكامل».

| المحور | التفصيل |
|---|---|
| **الفكرة** | وكيل بحساب (user token) يستمع لرسائل لوبيات بوتات الألعاب (Fizbo وأمثالها): شخص يكتب `-مافيا` → البوت يرسل لوبي بزر دخول → الوكيل يرى الرسالة و**يضغط الزر بنفسه** ثم يلعب ويفكر ويشارك مع اللاعبين |
| **المحركات الأربعة (منقول حرفياً)** | 🎰 **زر** (إرسال `-روليت` + الزر الأخضر عند «اضغط على الزر» + إعادة الإرسال عند كل فوز) • 🎡 **روليت** (لوبيات روليت/العجلة → رقم شاغر عشوائي بتأخير بشري 1000-2000ms) • 🪑 **كراسي** (إيمبد كراسي → أول زر + جولة السحب عشوائياً فوراً) • 🧠 **ريبلكا** (إيمبد ريبلكا → أول زر + أسئلة الفئة/الحرف) |
| **قاموس ريبلكا** | منقول حرفياً (28 حرفاً × 5 فئات من Auto) + **إجابة ذكية اختيارية** (مزود الوكيل نفسه، مهلة 7 ث، تنظيف إجابة) — القاموس هو الاحتياط الدائم، والتوقيت البشري المنقول: (طول الكلمة × 325) + 825ms |
| **البنية** | وحدة معزولة `games/`: engines.js (سجل المحركات) + policy.js (سياسة Auto: سيرفرات/بوتات/أقفال تداخل) + store.js (إعدادات كل وكيل×سيرفر + إحصائيات + كاش حي 30 ث) + events.js (المعالجات المنقولة) + player.js (خط الأنابيب) + panel.js (اللوحة) |
| **اللوحة /الألعاب** | على نمط Auto لكن بتصميم المنصة وتجربة /الرصد: اختر الوكيل ← اختر السيرفر ← صفحة الألعاب: مفتاح رئيسي + تشغيل/إيقاف لكل محرك + قناة وأمر حلقة زر + خيارات الذكاء + الانضمام المميز + إحصائيات الجلسة وآخر الأحداث + صفحة سياسة عامة (بوتات/سيرفرات/قفل) |
| **الانضمام المميز** | نسختا premium join من Auto منقولتان كاملتان لكن **معطلتان افتراضياً** (تنقران أي زر بعد ذكر اسم اللعبة — خطرة على وكيل يشارك المحادثة) — خيار لكل محرك من اللوحة |
| **صفر كسر (شرط المالك الصارم)** | ① الافتراضي معطل كلياً — بلا تفعيل صريح لا يلتقط الوكيل أي لعبة (المسار السريع يرجع فوراً) ② «كتم الذكاء على رسائل الألعاب» معطل افتراضياً — رسائل البوتات توصل للذكاء كما اليوم تماماً ③ وكلاء bot-token لا تُلحق بهم أحداث اللعب إطلاقاً (clickButton غير موجود لديهم) ④ بلا مستمعات متشعبة على العميل المشترك (حلقة زر تُدار من خط الأنابيب) ⑤ كل أخطاء الوحدة مبتلعة — لا تصل لمسار المحادثة أبداً ⑥ أوامر اللوحة على بوت المدير حصراً |
| **انحراف موثق عن Auto** | فلتر البوتات الافتراضي معطل (Auto مفعّل لأن حساباته مكرّسة للألعاب) — المالك يريد «اكتب -مافيا فيلعب فوراً» بلا تهيئة معرفات؛ التشديد متاح من صفحة السياسة |

**الاختبارات**: games_player.test.js جديد (15 مجموعة): الافتراضي معطل بصفر ضغطات (إثبات صفر كسر)، المحركات الأربعة، دخول كراسي/ريبلكا/روليت (استثناء اخرج/متجر/المعطلة)، الزر الأخضر من messageUpdate، قفل التداخل يحجب حساباً ثانياً ثم يحرره الفوز، فلتر البوتات، القاموس المنقول (س+دولة=سوريا، م+جماد=مقص، لا يوجد→null)، أولوية الذكاء وسقوطه للقاموس وتنظيف إجابته، كتم الذكاء بالصيغتين، عزل الإعدادات، وكلاء البوتات بلا فعل، وصفحات اللوحة الخمس بلا رميات. المجموعة الكاملة: 24 سويت.

## v7.14.1 — إصلاح «/الألعاب تظل تحمل» 🐞

**بلاغ المالك**: «امر /الألعاب لا يعمل — عندما ارسله يبقى يحمل ويحمل ولا يرسل اي شيء».

| المحور | التفصيل |
|---|---|
| **الجذر** | في `games/panel.js`: معالج الأمر كان يفعل `deferReply()` ثم يعود فوراً `return true` — أي يخبر ديسكورد «قادم بالرد» ثم **لا يرسل اللوحة أبداً** — الـ«يحمل» إلى الأبد |
| **الإصلاح** | تأجيل فوري (حماية مهلة ديسكورد 3 ثوان) ثم بناء وإرسال صفحة اختيار الوكيل فعلياً — نفس نمط /الرصد المجرّب (v7.11/v7.12) — وعند أي فشل: لوحة خطأ حمراء Components V2 بدل الصمت |
| **الصرامة** | اختبار الانحدار الجديد أُثبت أنه **يفشل فعلاً مع الكود القديم** (فحص: اللوحة أُرسلت بعد التأجيل) وينجح مع المصلّح — لا اختبار شكلي |

**الاختبارات**: games_player.test.js صار 16 مجموعة — الجديدة: الأمر يرسل اللوحة (defer+editReply بـ agent_select وعلم V2)، فشل القاعدة يرسل لوحة خطأ («DB down» ظاهرة)، والأوامر الغريبة تمر للوحة المدير بلا اعتراض. المجموعة الكاملة: 24 سويت خضراء.

## v7.15.0 — «يلعب حقاً ويتكلم كالبشر» 🎮🫧 — بلاغات المالك الثلاثة

**بلاغ المالك**: ① رسالة «🛰️ جاري البحث عن لاعبين خارجيين...» تُسجّل الوكيل كخسارة فلا يضغط شيئاً عندما يحين دوره ② بوت البادئة «.» يضغط زر «حقيبتي» بدل زر الدخول الأخضر ③ «نستطيع فعل ذلك؟» — تفاعل اجتماعي يشبه لاعباً بشرياً.

| المحور | التفصيل |
|---|---|
| **① الخسارة المزيفة — جذر مزدوج** | **(أ) معالج دور الروليت كان مفقوداً!** Auto يملك `Roulette/events/roulettePlay.js` وحناقطعن في v7.14 — لهذا «لا يضغط اي شيء عندما يأتي دوره». نُقل كاملاً: استهداف عشوائي بين اللاعبين + استبعاد انسحاب/طرد مرتين + محاكاة بشرية حرفية (800-2500ms + 10% تأخير أطول 3-6 ث + 1% تخطي دور) + **بوابة جلسة حية** (أشد من Auto: لا ضغط خارج لعبة انضم إليها فعلاً). **(ب) كشف النتيجة كان فضفاضاً**: «إذا خسرت ستفقد نقاطك» في إيمبد اللوبي + منشن قائمة اللاعبين = خسارة وهمية. الآن: رسائل اللوبي/البحث مرفوضة كلياً + فحص سطراً سطراً بحدود حقول محفوظة + رفض الصيغ الشرطية (يخسر/ستخسر/إذا خسر) + المطرود هو **أول منشن بعد العبارة فقط** (طرد غيرنا في سطرنا ≠ خسارتنا) + «تم طردك» بالمخاطبة المباشرة (mentions) + «الفائز:» للفوز |
| **② زر الدخول الذكي** | كان عشوائياً (يستثني اخرج/متجر فقط) فصادف «حقيبتي». الآن سلم أولويات: اسم انضمام صريح (انضمام/انضم/دخول/ادخل/اللعب) ← ثم عشوائي بين الأخضر (SUCCESS) ← ثم أول زر خارج قائمة سوداء موسعة (حقيبتي/محفظتي/معلومات/قوانين/رصيد/نقاط/مساعدة/تذكير/إحصائيات/حسابي/ملفي/انسحاب/شراء/حالة/الترتيب...) + رسائل الدور الموجهة للوكيل لا تعامل كلوبي دخول |
| **③ التفاعل الاجتماعي (افتراضي معطل)** | `games/social.js` + `games/sessions.js` (جلسات حية 15 د + حلقة كلام القناة 12 سطراً + أصدقاء = من ذكر اسمه أو منشناه + مطرودون). متى يتكلم: **طُرد هو** (90% — يسأل السبب) • **فاز** (60%) • **خسر** (70%) • **طُرد صديق الجلسة** (50% — يمزح كما قال: «لو تم طرد شخص انا صديقه اتكلم لكي اضحك عليه») • **ذكروا اسمه بلا منشن** (85% — «يراقب الشات ليرى ان تحدثو عنه»). متى يصمت: تبريد 60-120 ث بين كل كلمتين + سقف 6 كلمات للجلسة + «ان لم يتكلم شخص لا اتكلم» (الكلام التلقائي محصور بذكر اسمه — الأحداث وحدها تكسر الصمت) + **المنشن المباشر @هو يبقى للمحادثة الرئيسية** (بلا ازدواج ردود). الذكاء: مزود الوكيل + شخصيته + سياق الجلسة (مهلة 8 ث) والسقوط دائماً لجمل عربية جاهزة — ولا يذكر أبداً أنه ذكاء اصطناعي |
| **إصلاح خفي إضافي** | `processOutcome` كان يستدعي `clearLocks()` — يحرر أقفال **كل** الوكلاء على المنصة عند نتيجة وكيل واحد! أصبح `releaseLocksForAgent(agentId)` مُقيّداً |
| **التحكم** | زر 🫧 في صفحة الألعاب لكل سيرفر: التفاعل الاجتماعي مفعّل/معطل — والإعداد محفوظ في القاعدة ومنفصل عن المفتاح الرئيسي (تفعيله لا يشغل اللعب وحده) |

**الاختبارات**: games_player.test.js ← 21 مجموعة (+5: الخسارة المزيفة برسالة البلاغ الحرفية، النتائج الصحيحة بكل صيغها، زر حقيبتي، معالج الدور بجلسة/بلا جلسة/تخطي، القفل المُقيّد). games_social.test.js جديد (10): صفر كسر افتراضي، تعليق طرد بالذكاء عبر خط الأنابيب، التبريد، السقوط للجمل الجاهزة، ذكر الاسم بلا منشن مقابل المنشن المباشر، الصمت بلا جلسة، مزح الصديق وصمت الغريب، سقف الجلسة، عزل الوكلاء، حفظ الإعداد. المجموعة الكاملة: 25 سويت كلها خضراء (EXIT 0).

## v7.16.0 — لعبة المافيا الكاملة + وضع الذكاء/التلقائي 🕵️ — بلاغ المالك

**بلاغ المالك**: رسائل بوت المافيا (توزيع الرتب «✅ | تم توزيع الرتب على اللاعبين»، ليل القتل «🔪 | جاري انتظار المافيا لاختيار شخص لقتله»، الطبيب «💊 | جاري انتظار الطبيب لاختيار شخص لحمايته»، القتل «⚰️ | نجحت عملية المافيا وتم قتل <@X> وهذا الشخص كان مواطن»، النقاش «🔍 | لديكم 15 ثانية للتحقق بين اللاعبين»، والتصويت بأزرار أسماء اللاعبين) — مع طلبات: يتفاعل كأنه شخص حقيقي (يتمنى مافيا/يلعن حظه، يترجى ألا يُقتل، يقول «احميني»، يندب قتلى أصدقائه ويوعد بالثأر، يشك في الصامتين)، ويقرأ رسائله **السرية** (اختيار ضحية/حماية على الخاص أو المخفية)، ويصوّت بالأزرار، و«اريد وضعن للمافيا واحد يتحكم فيه الوكيل بالتصويت... والآخر يكون تلقائي» — ونفس الشيء للروليت «وضع الذكاء الاصطناعي هو من يختار يطرد ووضع تلقائي وهو الحالي».

| المحور | التفصيل |
|---|---|
| **المحرك الخامس** | `mafia` في engines.js — يظهر تلقائياً في لوحة /الألعاب بأزرارها وسياساتها (فلتر بوتات/سيرفرات/قفل تداخل). دخول اللوبي: رسالة تحوي «مافيا» + زر دخول ذكي (سلم v7.15: انضمام صريح ← أخضر ← خارج القائمة السوداء) — **ولا تُعامل رسائل المراحل كلوبي** (توزيع/انتظار/قتل/تحقق/تصويت مستبعدة صراحة) |
| **يعرف من يلعب معه** | «رسالة اللوبي يجب أن يتم إرسالها للوكيل اولا لكي يعرف من بلعب معه بالضبط وكذلك العدد» — منشنات اللوبي تُسجل في الجلسة (اسم/عدد/حي-ميت/عداد كلام) وتصل الذكاء في كل قرار |
| **المراحل والذاكرة** | sessions.js: `mafia = { role, phase, players, lastVictim }` + عدّاد كلام لكل لاعب (يعمل حتى بلا تفاعل اجتماعي). المراحل: lobby ← roles ← night_kill ← night_save ← day (قتل) ← day_discuss ← day_vote ← النهاية |
| **الرسائل السرية** | «الرسائل تأتي... تكون فقط مرئية للحساب وحده بمعنى مخفيه»: مساران — **الخاص** (agentRuntime أصبح يمرر رسائل الخاص على محرك الألعاب قبل التحويل — والتحويل يبقى دائماً ليرى المالك الدور) و**المخفية ephemeral** داخل القناة (flags 64). التعرف بصيغ كثيرة «باحتمالات كثيرة» كما طلب: قوائم KILL_MARKERS/SAVE_MARKERS (اختار/اختر/اختيار + اغتيال/قتل/حماية/علاج/دور الطبيب...) — ويُستخرج الدور من الرسالة (مافيا/طبيب/محقق/مواطن) ويُسجل للجلسة |
| **وضع الذكاء/التلقائي** | لكل وكيل×سيرفر: `engines.mafia.mode` و `engines.roulette.mode` = `auto` (الافتراضي — العشوائي، النظام الحالي صفر كسر) أو `ai` (الذكاء يقرر). زر في اللوحة (🕵️ وضع المافيا / 🎡 وضع الروليت). في الذكي: القتل (الأخطر/المخطِر، لا صديقاً)، الحماية (صديق/الأفيد)، التصويت (المشتبه به/الصامت)، الطرد في الروليت — بسياق كامل (الدور، الأحياء، عدّادات الكلام، الصامتون، الأصدقاء، آخر قتيل) + مهلة 6.5ث + فشل الذكاء → عشوائي دائماً + ضغط بتأخير بشري (0.9-1.8ث) |
| **الشخصية التفاعلية** | كل ردود الفعل باحتمالات + تبريد + سقف الجلسة (نظام v7.15) وأنواع جديدة: **توزيع الرتب** (80% — يتمنى مافيا/يلعن حظه بدوره المعروف أو عاماً) • **ليل القتل** (55% — يرجى ألا يُقتل) • **دور الطبيب** (50% — «احميني» بصيغ) • **قتل صديق** (65% — يندب القاتل ويوعد بأخذ حقه مع منشن القتيل) • **نقاش النهار** (45% — رأي يشك في الصامتين بأسمائهم) • **موت هو** (70% — تعليق ساخر) — **قاعدة صمت المافيا**: إذا صار دوره مافيا فكل هذا يتقلص ×0.12 (المافيا «يميل نوعا ما لعدم التكلم» كما لاحظ المالك) ولا يرجى أبداً في دور قتله |
| **النتائج الصحيحة** | «تم قتل <@هو>» = خسارة من نوع killed (الجلسة تُغلق والأقفال تُحرر — الميت لا يكمل) — وقيل صديقه فليست خسارتنا. إعلان الفائزين بقائمة منشنات («الفائزون: <@a> <@هو>») = فوز حتى لو لم يكن أول منشن |
| **لا ازدواج** | رسالة التصويت تتعدل (العدّاد ينزل لصفر) — `votedMessages` يمنع التصويت مرتين على نفس الرسالة (messageCreate + messageUpdate) |
| **التصويت** | «يجب على الوكيل الضغط على زر على اللاعب الذي يريد التصويت عليه»: أزرار الأسماء تُسجل لاعبين + استبعاد نفسه وأزرار الواجهة (حقيبتي/متجر/ميت/خارج...) + قرارات ذكية أو عشوائية حسب الوضع |

**الصرامة (إثبات فشل الكود القديم)**: أزلت «تم قتل» من عبارات الخسارة → اختبار 7 فشل فعلاً؛ عطلت الوضع الذكي في الروليت → اختبار 18 فشل؛ ألغيت قاعدة صمت المافيا → اختبار 17 فشل — ثم استعدت الكود فخرج كل شيء أخضر.

**الاختبارات**: games_mafia.test.js جديد — 21 فحصاً (صفر كسر، اللوبي+تسجيل اللاعبين، الأدوار، ليل القتل + صمت المافيا، الطبيب، قتل الصديق، موته، التصويت الذكي/التلقائي/لا ازدواج، السرية على الخاص مافيا/طبيب/بلا جلسة، الفائزون، محاكمة الصمت، التخزين، قاعدة الصمت، الروليت ذكي/تلقائي). games_player.test.js حُدث لقائمة المحركات الخمسة. المجموعة الكاملة: **26 سويت كلها خضراء (EXIT 0)**.

## v7.16.1 — إصلاح اختيار السيرفر في /الألعاب (فشل صامت) 🐞 — بلاغ المالك

**بلاغ المالك**: «لماذا في امر لألعاب في خطوة اختيار سيرفر، عندما اختار سيرفر يحدث خطأ؟ لا يتم اختيار؟ لم تكن موجودة سابقا انظر لماذا حدثت المشكلة هذه».

**التشخيص (انحدار من v7.16.0 نفسها — لذا «لم تكن موجودة سابقاً»)**:
- v7.14.0 نقل 4 محركات → صف أزرار مركز الألعاب كان: زر التشغيل + 4 محركات = **5 مكونات بالضبط** (الحد الأقصى المقبول في ديسكورد للصف الواحد) ✓
- v7.16.0 أضاف مافيا → الصف أصبح 6 مكونات → **ديسكورد يرفض بناء الصفحة كلياً** (HTTP 400 `Invalid Form Body`) عند أي اختيار سيرفر
- **لماذا الصمت التام؟** ديسكورد.js لا يتحقق محلياً من حد الـ5 (أُثبت بالتجربة الحية) — الرفض يحدث على الـAPI فقط، ودالة `update()` كانت تبتلعه بـ`.catch(() => {})` → لا لوحة خطأ ولا سطر في اللوق — فقط «فشل التفاعل» عند المالك
- وبالمثل كانت كل أزرار صفحة الألعاب (تشغيل/محركات/أوضاع/تحديث) ستفشل بنفس الطريقة لأنها تعيد بناء نفس الصفحة المكسورة

| الإصلاح | التفصيل |
|---|---|
| **تقسيم تلقائي للصفوف** | `renderGamesPage`: زر التشغيل + أول 4 محركات في الصف الأول، وفائض المحركات يُقسم آلياً على صفوف (5 لكل صف) — أي عدد محركات مستقبلاً يعمل بلا تكرار للخطأ |
| **لا صمت بعد اليوم** | `update()`: الفشل يُسجل دائماً في اللوق (`فشل تحديث اللوحة`) + إن بقي التفاعل بلا إقرار يُرسل بديل مرئي (لوحة خطأ حمراء بعنوان الخطأ وزر رجوع) بدل «فشل التفاعل» الصامت |
| **صفر مساس آخر** | games/panel.js حصراً (دالتان) + اختبار — لا شيء آخر تغير |

**الصرامة (إثبات فشل الكود القديم)**: استرجعت panel.js القديم مؤقتاً → المجموعة 22 فشلت فعلاً برسالة «صفحة الألعاب تُقبل من ديسكورد فعلاً — Invalid Form Body» (محاكاة ديسكورد الحرفية ترفض صف 6 مكونات كما رفض الـAPI فعلاً) → استعدت الإصلاح → 22/22 خضراء.

**الاختبارات**: games_player.test.js ← مجموعة 22 جديدة بثلاث حالات: (أ) الصفحات الأربع كلها (اختيار وكيل/سيرفر/ألعاب/سياسة) تمر على فاحص يحاكي ديسكورد حرفياً (رفض صف > 5 مكونات أو رسالة > 40) بلا أي رفض (ب) المسار الكامل: اختيار سيرفر من القائمة → صفحة مركز الألعاب تُرسل وتُقبل فعلاً (ج) حزام الأمان: فشل الإرسال مستقبلاً → تسجيل + بديل مرئي — لا صمت. المجموعة الكاملة: **29 سويت كلها خضراء (EXIT 0 — 276 فحصاً)**.

## v7.18.0 — الصندوق الحي: الوكيل يرى اللعبة بعينيه، يختار بعقله، ويعرف نفسه 📥 — بلاغ المالك

**بلاغ المالك الحرفي**: «حاليا خدعة رخصية فقط — لم تفي بما طلبته انا ابدا. انا طلبت بأنه اي رساله من اللعبة يتم إرسالها للوكيل ليرد او لا — مثلا اللوبي يتم إرسالها ومع الأعضاء المشاركين وليس فقط النص — حاليا يتم ارسال النص للوبي لكن لا يتم ارسال اسماء اللاعبين في الجولة — لا اريده يحاكي انه يعرف ما يجري بل اريده فعلا يعرف ما يجري ومن يلعب معه لأنني حاليا لو سألته من في اللعبه او بمن تشك لا يعرف اصلا — حاليا فقط اول مرة اتكلم معه يتم ارفاق له رساله اللوبي — اخخخ لدرجة فزت معه انا مافيا هو لا يعرف اصلا ويقول انه مواطن — لا يختار هو اصلا من يقتل او على من يصوت — انت فقط تعطيه معلومات وحتى معلومات ناقصة» + إشعارات مفصلة وقناة ألعاب منفصلة لكل وكيل + جلب رسائل أي سيرفر + استقلالية الأدوات («اريده وكيل حقيقي!»).

**التشخيص بالكود (لماذا بقي فاشلاً في v7.17.0)**:
| البلاغ | الجذر المثبت |
|---|---|
| «لا يتم ارسال اسماء اللاعبين» | collectMentions كان يقرأ message.mentions وحدها — منشنات الإيمبد لا تدخل فيها أصلاً (نفس جذر «البوت فاز») + لا معالج لتعديل اللوبي حين ينضم الآخرون لاحقاً |
| «يقول مواطن وهو مافيا» | نص الرسالة السرية كان يُبنى من content + title/desc فقط — **حقول الإيمبد (fields) مستثناة** فبطاقة الدور لا تُكتشف والدور يبقى مجهولاً والعقلة تخمّن |
| «فقط أول مرة اتكلم معه» | TTL الجلسة 15 دقيقة ولمسات التجديد نادرة — مراحل الليل (رسائل خاصة) صمت قناتي طويل فتموت الجلسة وتُمحى كل بيانات اللعبة |
| «لا يختار هو من يقتل/يصوت» | الوضع التلقائي = عشوائي حرفياً، والذكي يسقط عشوائياً بصمت عند أي فشل |
| «يتكلم بمفرده» | جمل جاهزة مبرمجة لا تعرف اللعبة |
| «الإشعارات لا أفهمها» | notify() كان يعرض معرف الوكيل، بلا اسم/سيرفر/شات/وقت/لعبة |
| «جلب الرسائل سيرفر واحد» | target_guild محصور owner والقناة لا تُبحث في باقي سيرفرات العميل |

**ما بُني في v7.18.0**:
1. **📥 الصندوق الحي (sessions.js)**: كل رسالة بوت اللعبة (قناة أو خاص) تُنسخ **حرفياً** لعقل الوكيل (inbox آخر 14 رسالة × 500 حرف، بمنع التكرار بمعرف الرسالة) — تنسخ في player.js قبل أي معالجة، وتُبث في برومبتات القرارات والكلام وسياق المحادثة الرئيسية — «لا ملخصات ولا تخمين».
2. **الأسماء الحقيقية (events.js)**: extractPlayerMentions تجمع منشنات content **ومنشنات الإيمبد** (regex) وتحلل الأسماء من السيرفر — + معالج جديد mafiaLobbyUpdate (messageUpdate) يحدّث قائمة اللاعبين ونص اللوبي كلما تُعدلت رسالة اللوبي (لاعبون انضموا بعدنا).
3. **الدور من كل صيغه (mafia.js)**: secretText يشمل حقول الإيمبد + footer + Components V2 — بطاقة الدور داخل حقل تُكتشف، تُسجل، تدخل الصندوق، ويقول عقله «عرفت دورك» — ورد فعل حقيقي بالذكاء في قناة اللعبة (المافيا تخضع لقاعدة الصمت ×0.12 — لا تكشف نفسها).
4. **القرار دائماً بعقله**: القتل/الحماية/التصويت/الطرد يستشيرون ذكاء الوكيل **في الوضعين** — العشوائي احتياط فشل فقط — والبرومبت يتضمن نص رسالة الاختيار الحقيقية + الخيارات (الأزرار) + الدور + الصامتون — «رساله الاختيار يتم إرسالها للوكيل مع الخيارات وهو يختار والنظام يطبق».
5. **الوضعان يختلفان فعلاً**: `اجتماعي 🫧` (يلعب ويتفاعل بالكلام كاملاً) ↔ `تلقائي 🤫` (يلعب ويقرر بعقله لكن يكتم الشات — ردود موقعه فقط: دوره/موته/فوزه/طرده) — 'ai' القديم يُقرأ اجتماعياً (توافق) — الأزرار والتخزين واللوحة محدثة.
6. **رد اللوبي**: في الوضع الاجتماعي يرد **على رسالة اللوبي نفسها** (messageReference) باحتمال 60% — «يستطيع أن يعمل رد على رسالة اللوبي ام لا».
7. **موت «فقط أول مرة» انتهى**: TTL الجلسة 45 دقيقة، وكل دالة وعي/رسالة لعبة تُجدد العمر — مراحل الليل لا تمحو وعيه.
8. **الهوية**: session.me (id+name) تُسجل عند الانضمام — السياق يقول حرفياً «أنت اللاعب «X» ومعرفك <@id> — كل منشن لك يعني أنت» + «أنت مافيا: حاول ألا تكشف نفسك».
9. **إشعارات مفصلة + قناة ألعاب منفصلة**: notify() يعرض اسم الوكيل (لا معرفه) + اسم السيرفر + الشات بمنشنه + اللعبة + الوقت — وإعداد `game_notification_channel_id` لكل وكيل من لوحة المدير (قناة إشعارات الألعاب المنفصلة عن العامة) تُوجَّه إليه أحداث game_player أولاً.
10. **أدوات وكيل حقيقي**: get_messages/search_messages/pinned تبحث عن القناة في **كل سيرفرات العميل** إن لم توجد محلياً + target_guild صار متاحاً للأدمن (قراءة فقط) — + قسم «المبادرة الذاتية» في برومبت النظام بنسخه الأربع: «لا تنتظر طلباً — سُئلت عن لعبة سابقة لا تعرفها؟ اقرأ رسائل القناة الآن» (المثال الحرفي من بلاغ المالك).

**الاختبار (32 سويت خضراء — EXIT 0)**:
- `tests/games_inbox.test.js` جديد (10 مجموعات): الأسماء من الإيمبد (كانت 0) / تعديل اللوبي يحدّث القائمة / الدور من حقل إيمبد / الصندوق الحي حرفياً بلا تكرار / قرار القتل بعقله حتى في التلقائي + نص الرسالة في البرومبت / الوضعان مختلفان / رد اللوبي messageReference / حياة الجلسة 20 دقيقة صمت حية و46 تنتهي / الهوية / «من في اللعبة؟» يُجاب بالأسماء والحالة والعدد.
- `tests/agent_autonomy.test.js` جديد (4 مجموعات): قسم المبادرة الذاتية في النسخ الأربع + المثال الحرفي + توثيق القراءة العابرة للسيرفرات + توازن صلاحية admin.
- تحديث games_mafia (9/16/18.ب: التلقائي صار قرار العقل — لا عشوائي) وgames_awareness (1/3: بوابة النوع للوضعين) وnotify_routing (6: قناة الألعاب المنفصلة).
- **إثبات صرامة بالاسترجاع**: git stash → `games_inbox` فشل حرفياً: «أسماء اللاعبين من الإيمبد سُجلت (كانت 0) — وجدنا 0» (بلاغ المالك نفسه) و`agent_autonomy` فشل: «قسم المبادرة الذاتية موجود» → الاستعادة → أخضر كامل.
- npm test كامل: **EXIT 0 — 32 ملف اختبار**.

---

## v7.17.0 — وعي الوكيل باللعبة: اللوبي تصل لعقله، الذكي يتفاعل فعلاً، والاحتساب الصحيح 🧠 — بلاغ المالك

**بلاغ المالح الحرفي**: «إلى الآن هو فاشل — عند بدأ لعبه ويدخل لها اريد رسالة اللوبي نفسها يتم إرسالها للوكيل وهو حاليا لا يفعل ذلك — ووضع التلقائي او الاجتماعي لا يوجد اختلاف بيهم الاثنان لا يتفاعلون — ولا يعرف اللعبة ولا اعرف انه الان يلعب — ولا يعرف اللعبة اصلا ولا يعرف ماذا يحصل بها كل شيء لا يعرف — وكذلك عندما يتم قتله ومنشنته او يفوز يحسب انه البوت الذي فاز وليس هو من فاز — هو فقط يلعب تلقائي لا يلعب اصلا ولا يعرف انه يلعب».

**التشخيص بالكود (لماذا كان كل شيء ميتاً)**:
| البلاغ | الجذر في الكود |
|---|---|
| «رسالة اللوبي لا تُرسل للوكيل» | mafiaJoin ضغط الزر ورما النص — كان يخزن معرفات اللاعبين فقط في بنية داخلية، والنص نفسه كان يضيع — لم يصله الذكاء أبداً |
| «لا يوجد اختلاف بين التلقائي والاجتماعي» | كلام المراحل كله كان خلف زر التفاعل الاجتماعي (معطل افتراضياً) — والوضع الذكي كان يغير فقط من يختار زر التصويت/القتل بلا أي كلام، وفشل الذكاء كان يسقط للعشوائي **بصمت تام** — فبدا الوضعان متطابقين وميتين |
| «لا يعرف ماذا يحصل بها» | المراحل كانت تُطابق وتُخزن في ذاكرة داخلية لا يقرأها أحد — ولا شيء منها يصل لعقل المحادثة الرئيسية حين يكلّمه أحد وهو يلعب |
| «يحسب انه البوت الذي فاز» | قوائم الفائزين داخل **الإيمبد** كانت تضيع (منشنات الإيمبد لا تصل message.mentions والفحص كان يتطلبها) + نصوص الأحداث كانت غامضة («رسالة بوت تحتوي: الفائز») — لا يُعرف من فاز |
| «لا اعرف انه الان يلعب» | لا شيء مرئي في اللوحة أو الأحداث يبين جلسة حية |

| الطبقة | التفصيل |
|---|---|
| **🧠 الوعي (sessions.js)** | كل جلسة تحمل الآن: `lobbyText` (نص رسالة اللوبي الحقيقي حتى 400 حرف) + `events` (سجل آخر 8 أحداث) — ويُبثان في كل برومبت ذكاء (قرارات + كلام) وفي سياق المحادثة الرئيسية |
| **اللوبي → العقله فعلاً** | mafiaJoin يلتقط النص، player.js يخزنه في الجلسة، ويظهر حرفياً في: برومبت التصويت/القتل/الحماية، برومبت الكلام الاجتماعي، وسياق «أنت داخل لعبة جارية» حين يكلّمه أحد — «نريد جعله قوي» |
| **سجل «ماذا يحصل»** | كل مرحلة تُسجل بأسماء: دخلنا اللوبي (عدد اللاعبين) ← توزيع الرتب ← ليل القتل ← قتلت المافيا «باسم القتيل» ← نقاش النهار ← صوّت على طرد «باسم الهدف» (قرار الذكاء أو عشوائي) ← الحركات السرية ← النتيجة |
| **الوضع الذكي = لعب واعي** | `speechAllowed`: زر 🫧 **أو** وضع ذكي — الوضع الذكي (مافيا/روليت) يفعّل كل كلام المراحل (تمني المافيا/الترجي/طلب الحماية/الشك/الندبة) حتى لو كان زر التفاعل الاجتماعي معطلاً — والتلقائي يبقى كما هو صامتاً (صفر كسر) |
| **فشل الذكاء مرئي** | كل سقوط للعشوائي/الجمل الجاهزة يُسجل الآن: لوق + «🧠 استشارة الذكاء فشلت (الموضع) — السبب: ...» في آخر الأحداث (بسقف تبريد 3 دقائق) — لن يبدا الذكي = التلقائي مجدداً بلا سبب مرئي |
| **الاحتساب الصحيح 🏆** | `winnersLineIncludesMe`: قائمة الفائزين في نفس السطر فيها منشننا (أو معرفنا بحدود أرقام) = فوزنا — تعمل مع الإيمبدات بلا اعتماد على mentionsMe. والنصوص لا لبس فيها: «🏆 الوكيل «اسمه» فاز فعلاً» / «💀 الوكيل «اسمه» قُتل في المافيا» / «💀 طُرد» — وليس «رسالة بوت تحتوي» |
| **عقل المحادثة يرى اللعبة** | agentRuntime يحقن `buildLiveGameContext` بعد الذكريات: «أنت داخل لعبة جارية الآن — اللوبي الذي انضممت بعدها: ... — اللاعبون معك: ... — دورك ومرحلتك — آخر ما حدث» — بلا جلسة → null (صفر تغيير) |
| **المالك يراه** | لوحة /الألعاب تعرض «🟢 الجلسة الحية: يلعب الآن — اللعبة والمدة واللاعبون والدور والمرحلة» |
| **إصلاح خفي** | عدّاد كلام اللاعبين (سبب الشك في الصامتين) كان خلف بوابة زر 🫧 — يعني بلا زر كان ميتاً والصامتون لا يُكتشفون — صار يعمل داخل أي جلسة حية |

**الصرامة (إثبات فشل الكود القديم)**: استرجعت كل الملفات المعدلة عبر git stash → الاختبار الجديد فشل فعلاً (`social.speechAllowed is not a function` — الميزة لم تكن موجودة أصلاً) → استعدت الإصلاح → أخضر.

**الاختبارات**: games_awareness.test.js جديد — 8 مجموعات تغطي بلاغ المالك بنداً بنداً: صفر كسر تلقائي، اللوبي محفوظة حرفياً + اللاعبون بالأسماء، سجل المراحل باسم القتيل، الذكي يكلم مع زر معطل (الفرق الحقيقي)، اللوبي داخل برومبت الذكاء حرفياً + اختيار الهدف، فشل الذكاء مرئي بسببه، الاحتساب (فوزنا في إيمبد/فوز غيرنا/قتلنا/قتل غيرنا) بنصوص لا لبس فيها، وسياق المحادثة الحي (وnull بلا جلسة). المجموعة الكاملة: **30 سويت كلها خضراء (EXIT 0)**.

---

## v7.19.0 — جولة التصويت الحقيقية: يصوّت بأي صياغة، يرى الخيارات والأصوات، ويحسم بطاقاته 🗳️ — بلاغ المالك

**بلاغ المالك الحرفي**: «انا عامله اجتماعي لكنه لا يستطيع اللعب مثلا تصويت على طرد في جولة التصويت او طرد اذا كان مافيا. او اذا دكتور لانه حاليا تصله النتيجة فقط لا يعرف في جولات التصويت من صوت على من وخيار له لكي يصوت على احد لماذا؟»

**التشخيص بالكود (7 جذور مثبتة)**:
| البلاغ | الجذر المثبت |
|---|---|
| «لا يستطيع التصويت» | كشف التصويت مقفول على خمس صيغ حرفية (VOTE_TEXT) — أي صياغة أخرى من بوت اللعبة = لا تصويت أبداً |
| «وخيار له لكي يصوت على احد» | الصندوق الحي ينسخ النص فقط — أسماء الأزرار (الخيارات) لا تصل لعقله أصلاً |
| «لا يعرف من صوت على من» | تعديلات عدّاد التصويت تُنسخ نصاً خاماً — والعدد يعيش في أسماء الأزرار التي لا تُنسخ — ولا بنية أصوات إطلاقاً |
| «او اذا دكتور» | بطاقة الطبيب «اختر شخصاً لحمايته» كانت تُصنّف **قتلاً** (التفرقة تبحث «حماية» ولا تجد «حمايته») — عقله يستشير بذهنية مافيا |
| «طرد اذا كان مافيا» | برومبت التصويت يقول «صوّت على من تشك أنه مافيا» حتى وهو مافيا — يصوّت على زملائه! + لا يعرف زملاءه أصلاً |
| بطاقات القناة الظاهرة | معالجا ليل القتل/الطبيب يستهلكان الرسالة بـ break — بطاقة اختيار ظاهرة = لا أحد يختار أبداً |
| «عامله اجتماعي» | التصويت يضغط بصمت — المالك لا يرى أي دليل أنه يلعب |

**ما بُني في v7.19.0**:
| الطبقة | التفصيل |
|---|---|
| **كشف موسّع** | `looksLikeVote`: أي رسالة فيها (طرد/تصويت/صوتوا/أصوات/اقتصاص/محاكمة/حكم) + مرشحون حقيقيان على الأزرار (≥2) = تصويت — الصيغ القديمة الخمس تعمل كما هي — ورسالة التصويت التي تحتوي «مافيا» لم تعد تُفهم لوبياً (كانت تُضغط أزرارها عشوائياً كـ«انضمام»!) |
| **الخيارات لعقله** | `optionsLineFromComponents`: كل رسالة لعبة تُنسخ للصندوق الحي مع «الخيارات: [أحمد] [خالد] [سعيد]» (أزرار + قوائم اختيار) — في القناة والخاص والتحديثات — «وخيار له لكي يصوت» أصبح حقيقة |
| **لوحة الأصوات** | `mafiaVoteTally` (messageUpdate): يقرأ عدّاد الأصوات من أسماء الأزرار «أحمد (2)» / «سالم - 3» / «عمر ٢» (الأرقام العربية مدعومة) → `session.mafia.voteCounts` + حدث «لوحة الأصوات الآن: ...» — تظهر في سياق الجلسة وسياق المحادثة وبرومبت القرار نفسه |
| **ذهنية الدور في التصويت** | برومبت التصويت حسب الدور: مافيا = «أسقط مواطناً بريئاً ولا تصوّت على زميل والتزم الأغلبية حتى لا تنكشف» / طبيب ومحقق ومواطن بذكاءاتهم — وزملاؤه المافيا يُعلَّمون في القائمة «زميلك في المافيا — لا تختاره أبداً» |
| **زملاء المافيا** | بطاقة الدور «زملاؤك في الفريق: <@X> <@Y>» → `session.mafia.allies` — يظهرون في كل سياق («ومن معه») ولا يُقتلون ولا يُصوّت عليهم |
| **بطاقات القناة الظاهرة** | `handleSecretMessage(allowVisible)`: معالجا ليل القتل/الطبيب يحاولان الاختيار فعلاً من البطاقة الظاهرة (بأزرار) — مع حارسين: بلا أزرار = إعلان طور عامة (لا يغيّر الدور — كان يُسجّل «مافيا» من كلمة إعلان الطور!) + توافق الدور (بطاقة قتل وطبيبنا معروف = ليست لنا) |
| **الإعلان الاجتماعي 🗣️** | `maybeVoteAnnounce`: في الوضع الاجتماعي يعلن تصويته في الشات ببرر قصير (سقف 3 لكل جلسة، بلا تبريد الدقيقة لأن المهلة 15ث) — التلقائي يصوّت بصمت تماماً (الفرق الحقيقي بين الوضعين) |
| **ميت = متفرج** | طبقتا حماية: موته يغلق الجلسة (كما هو) وإن بقيت جلسة فـ`meDead` يمنع التصويت والحركات السرية — والأصوات تبقى تُراقب له |
| **زر «تحديث»** | ليس ضحية أبداً (كان مرشحاً ويُضغط بلا معنى) |

**الصرامة (إثبات فشل الكود القديم)**: git stash لكل التعديلات → الاختبار الجديد فشل فعلاً على بلاغ المالك الحرفي: «صوّت فعلاً بصياغة غير معروفة — actual: 0, expected: 1» (صفر ضغطات — لا يصوّت أبداً) → استعدت الإصلاح → أخضر.

**الاختبارات**: games_voting.test.js جديد — 11 مجموعة: صياغة تصويت غير معروفة تُصوَّت فعلاً / تصويت يحتوي «مافيا» لا يُفهم لوبياً / الخيارات تصل الصندوق الحي والسياق / عدّاد الأصوات من التعديلات (2→3) يصل عقله / تصنيف الطبيب=حماية / مافيا لا تصوّت على زميلها والبرومبت بذهنيتها / الزملاء من بطاقة الدور / بطاقة القتل الظاهرة تُحسم / الاجتماعي يعلن والتلقائي صامت / الميت متفرج بطبقتين + زر تحديث مستبعد / صيغ العدّاد الثلاث. المجموعة الكاملة: **33 ملف اختبار — 301 فحصاً ناجحاً — EXIT 0**.
